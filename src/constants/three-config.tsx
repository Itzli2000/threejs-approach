export const FOV = 75;
export const W_WIDTH = window.innerWidth;
export const W_HEIGHT = window.innerHeight;
export const ASPECT = W_WIDTH / W_HEIGHT;
export const NEAR = 0.1;
export const FAR = 1000;
export const DEFAULT_MESH_COLOR = 0x0000FF;
